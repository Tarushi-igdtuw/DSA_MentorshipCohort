public static Node insertAtBeg(int elt,Node head) { //Q1
      Node nnode=new Node(elt); 
      if(head==null) {     
         head=nnode;
      }
      else {
         nnode.next=head; 
         head=nnode;
      }
      return head;
}
